[project]
name = "rivals-smurf-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlmodel>=0.0.22",
    "textual>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "textual-dev>=1.7.0",
]
